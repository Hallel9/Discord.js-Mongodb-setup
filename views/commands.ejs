<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Fire-Cam-Bot cmds</title>
    </head>

    <body>
        <a href="/"<img
        class="avatar"
        src="https://cdn.discordapp.com/avatars/837874970432110653/b13afd46cb55245acc595aa273ed02b6.png"
        alt="Bot's avatar!"
      />></a>
      <section class="main-content">
          <p>To get a look at the commands in discord, use f?commands to view the commands.</p>

          <% commands.forEach(({ name, value }) => { %>
              <table style="width: 100">
                <h1 style="text-align: center;">
                    <% name %>
                </h1>
                <tr>
                    <th>Command Name</th>
                    <th>Category</th>
                    <th>Usage</th>
                    <th>Usage2</th>
                    <th>Description</th>
                    <th>Aliases</th>
                </tr>
              <%  value.forEach(({ name, description, aliases, usage, category, usage2 }) => { %>
                    <tr>
                        <td><%= name%></td>
                        <td><%= category%></td>
                        <td><%= usage%></td>
                        <td><%= usage2%></td>
                        <td><%= description%></td>
                        <td><%= typeof aliases === 'string' ? aliases : aliases.join(', ')%></td>
                    </tr>
                <% }) %>
              </table>
         <% }) %>
      </section>
    </body>
    <style>
        table {
            border-collapse: collapse;
            border-spacing: 0;
        }
        *  {
            box-sizing: border-box;
        }

        body {
            padding: 0;
            margin: 0;
            font-family:'Courier New', Courier, monospace;
            font-size: 16px;
            line-height: 1.5;
            color: #dd590d
        }
        
        .main-content {
            word-wrap: break-word;
        }

        @media screen and (min-width: 64em) {
            .main-content {
                max-width:64rem;
                padding: 2rem 6rem;
                margin: 0 auto;
                font-size: 1.1rem;
            }
        }

        @media screen and (min-width: 42em) and (min-width: 64em) {
            .main-content {
                padding: 2rem 4rem;
                font-size: 1.1rem;
            }
        }

        @media screen and (min-width: 42em) {
            .main-content {
                padding: 2rem;
                font-size: 1rem;
            }
        }

        .main-content h1,
        .main-content h2,
        .main-content h3,
        .main-content h4,
        .main-content h5,
        .main-content h6 {
            margin-top: 2rem;
            margin-bottom: 1rem;
            font-weight: normal;
            color: #dd590d
        }

        .main-content table th,
        .main-content table td {
            padding: 0.5rem 1rem;
            border: 1px solid #e9ebec;
        }

        .avatar {
      display: block;
      margin-left: auto;
      margin-right: auto;
      border: 4px solid #4caf50;
      border-radius: 100%;
      width: 20%;
      height: 50%;
      -webkit-transform: -webkit-transform 0.8s ease-in-out;
      transition: transform 0.8s ease-in-out;
    }
    .avatar:hover {
      -webkit-transform: rotate(360deg);
      transforn: rotate(360deg);
    }
    </style>
</html>
